rootProject.name = 'CDDDMIJava'

if (file("../../foundation/CDDBaselineJava").exists()) {
    includeBuild("../../foundation/CDDBaselineJava") {
        dependencySubstitution {
            substitute module('CDDBaseline:0.3') with project(':')
        }
    }
}
