jdk:
  - openjdk8
install:
  - echo "Running a custom install command"
  - cd foundation/CDDBaselineJava/ && ./gradlew clean installShadowDist -xtest && mv install/CDDBaselineJava-shadow/lib/CDDBaselineJava-*-all.jar .
